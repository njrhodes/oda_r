% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graphics_v3.R
\name{plot_smd_balance}
\alias{plot_smd_balance}
\title{Plot SMD covariate balance}
\usage{
plot_smd_balance(
  x,
  ref_010 = TRUE,
  ref_020 = FALSE,
  main = NULL,
  subtitle = NULL,
  palette = NULL,
  theme = c("clean", "minimal")
)
}
\arguments{
\item{x}{A \code{"smd_balance_table"} object from
\code{\link{smd_balance_table}}.}

\item{ref_010}{Logical; draw a dashed reference line at |SMD| = 0.10.
Default \code{TRUE}.}

\item{ref_020}{Logical; draw a dotted reference line at |SMD| = 0.20.
Default \code{FALSE}.}

\item{main}{Character; plot title.  Default \code{"SMD Covariate Balance"}.}

\item{subtitle}{Character; plot subtitle.}

\item{palette}{Named list for color overrides: \code{imbalanced},
\code{balanced}, \code{unclassified}.}

\item{theme}{Character; \code{"clean"} (default) or \code{"minimal"}.}
}
\value{
A \code{\link[ggplot2]{ggplot}} object.
}
\description{
Renders a horizontal dot-plot of absolute standardized mean differences
(|SMD|) for each covariate.  Vertical reference lines at 0.10 (and
optionally 0.20) mark conventional balance thresholds.  Points are colored
by whether |SMD| < 0.10.
}
\examples{
\donttest{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  group <- c(rep(0L, 20), rep(1L, 20))
  X     <- data.frame(A = c(rep(0L,20), rep(1L,20)),
                       B = rnorm(40))
  smd <- smd_balance_table(group, X)
  p   <- plot_smd_balance(smd)
  print(p)
}
}
}
\seealso{
\code{\link{smd_balance_table}}, \code{\link{plot_balance_love}}
}
