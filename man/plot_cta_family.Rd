% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graphics_v3.R
\name{plot_cta_family}
\alias{plot_cta_family}
\title{Plot a CTA descendant family member using ggplot2}
\usage{
plot_cta_family(
  family,
  index = 1L,
  min_d = FALSE,
  show_all = FALSE,
  layout = c("multipanel", "list"),
  ncol = 1L,
  target_class = 1L,
  color_by = c("none", "target_rate", "prediction"),
  label_detail = c("simple", "full"),
  show_node_ess = FALSE,
  show_p = TRUE,
  show_loo = TRUE,
  main = NULL,
  subtitle = NULL,
  show_rule = TRUE,
  show_metrics = FALSE,
  short_edge_labels = TRUE,
  node_text_size = 3.5,
  edge_text_size = 3.2,
  palette = NULL
)
}
\arguments{
\item{family}{A \code{cta_family} object from
\code{\link{cta_descendant_family}}.}

\item{index}{Integer or \code{"min_d"}; which family member to render.
Default \code{1L}.  Use \code{"min_d"} to render the member with minimum
D-statistic.  Ignored when \code{show_all = TRUE}.}

\item{min_d}{Logical; convenience shorthand for \code{index = "min_d"}.
When \code{TRUE}, renders the minimum-D family member regardless of
\code{index}.  Default \code{FALSE}.}

\item{show_all}{Logical; if \code{TRUE}, render all family members.
Output format is controlled by \code{layout}.  Default \code{FALSE}.}

\item{layout}{Character; \code{"multipanel"} (default, requires
\pkg{patchwork}) returns a single combined figure; \code{"list"} returns
a named list of ggplot objects.  Only relevant when \code{show_all = TRUE}.}

\item{ncol}{Integer; number of columns in the multipanel grid.  Default
\code{1L}.  Only used when \code{show_all = TRUE} and \code{layout =
"multipanel"}.}

\item{target_class}{Integer; target class for endpoint coloring (default
\code{1L}).}

\item{color_by}{Character; leaf-node fill.  \code{"none"} (default),
\code{"target_rate"}, or \code{"prediction"}.}

\item{label_detail}{Character; \code{"simple"} (default) or \code{"full"}.}

\item{show_node_ess}{Logical; append node ESS to split labels.
Default \code{FALSE}.}

\item{show_p}{Logical; append \code{MC p = X.XXX} to split-node labels.
Default \code{TRUE}.}

\item{show_loo}{Logical; append LOO status/p to split-node labels.
Default \code{TRUE}.}

\item{main}{Character; plot title.  Default: auto-generated with MINDENOM
and D.}

\item{subtitle}{Character; plot subtitle.}

\item{show_rule}{Logical; show edge condition labels.  Default \code{TRUE}.}

\item{show_metrics}{Logical; append ESS/D to subtitle.  Default \code{FALSE}.}

\item{short_edge_labels}{Logical; strip attribute prefix from edge labels.
Default \code{TRUE}.}

\item{node_text_size}{Numeric; text size for node labels.  Default \code{3.5}.}

\item{edge_text_size}{Numeric; text size for edge labels.  Default \code{3.2}.}

\item{palette}{Named list for color overrides.}
}
\value{
A \code{\link[ggplot2]{ggplot}} object (single member or multipanel),
  or (when \code{show_all = TRUE} and \code{layout = "list"}) a named list
  of ggplot objects.
}
\description{
Renders a publication-quality CTA tree diagram for a single member of a
\code{cta_family} object (indexed inspection), or a named list of plots for
all members (\code{show_all = TRUE}).  Requires the \pkg{ggplot2} package.
}
\examples{
\donttest{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  X <- data.frame(x1 = c(rep(0L,20), rep(1L,20)),
                  x2 = c(rep(0L,10), rep(1L,10), rep(0L,10), rep(1L,10)))
  y <- c(rep(0L,30), rep(1L,10))
  fam <- cta_descendant_family(X, y, mc_iter=200L, mc_seed=42L, loo="off")
  p   <- plot_cta_family(fam, index=1L)
  print(p)
}
}
}
\seealso{
\code{\link{cta_descendant_family}}, \code{\link{plot_cta_tree}},
  \code{\link{plot_lort_tree}}, \code{\link[ggplot2]{ggsave}}
}
