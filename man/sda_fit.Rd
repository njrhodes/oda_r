% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sda_core.R
\name{sda_fit}
\alias{sda_fit}
\title{Run a Structural Decomposition Analysis (SDA) procedure}
\usage{
sda_fit(
  X,
  y,
  mode = c("novometric_min_d", "unioda_max_ess"),
  attr_types = NULL,
  weights = NULL,
  mindenom = NULL,
  mc_iter = 5000L,
  mc_seed = 42L,
  mc_stop = 99.9,
  mc_stopup = 20,
  alpha = 0.05,
  loo = "off",
  max_steps = NULL,
  min_n = NULL,
  min_class_n = NULL,
  remove_correct = TRUE,
  collinearity = c("skip", "warn", "allow"),
  verbose = FALSE
)
}
\arguments{
\item{X}{Data frame of candidate attribute columns.}

\item{y}{Integer class vector. Must have exactly two distinct values.}

\item{mode}{SDA mode. \code{"novometric_min_d"} (MPE-canon; per-attribute
MDSA via \code{cta_descendant_family()}; requires \code{mindenom}) or
\code{"unioda_max_ess"} (iterative UniODA; default for SDA-1). Must be
declared explicitly; do not mix modes.}

\item{attr_types}{Named character vector of attribute types
(\code{"ordered"}, \code{"categorical"}, \code{"binary"}), or \code{NULL}
for auto-detection. Names must match column names of \code{X}.}

\item{weights}{Case weights. Must be \code{NULL} in SDA-1; weighted SDA is
not yet implemented and will error if non-\code{NULL}.}

\item{mindenom}{Integer MINDENOM (novometric mode only; ignored with warning
in unioda_max_ess mode).}

\item{mc_iter}{Maximum Monte Carlo iterations per attribute fit. Default 5000L.}

\item{mc_seed}{RNG seed set once before the SDA run. Default 42L.}

\item{mc_stop}{Lower-tail early-stop confidence (percent). Default 99.9.}

\item{mc_stopup}{Upper-tail early-stop confidence (percent). Default 20.}

\item{alpha}{Significance threshold for p-value gate. Default 0.05.}

\item{loo}{LOO mode passed to \code{oda_fit()}. Default \code{"off"}.}

\item{max_steps}{Maximum number of SDA steps (safety cap). Default \code{NULL}
(no cap beyond candidate exhaustion).}

\item{min_n}{Minimum working-sample size. If unresolved n drops below this,
stop with \code{"min_n"}. Default \code{NULL}.}

\item{min_class_n}{Minimum per-class count. Stop with \code{"min_class_n"}
if either class falls below this. Default \code{NULL}.}

\item{remove_correct}{Logical. If \code{TRUE} (canonical SDA), remove
correctly classified observations after each step. If \code{FALSE},
diagnostic dry-run: step logic executes but working sample is not modified.
Default \code{TRUE}.}

\item{collinearity}{How to handle duplicate candidate columns:
\code{"skip"} (silent), \code{"warn"}, or \code{"allow"}. Default
\code{"skip"}.}

\item{verbose}{Logical. Emit \code{[SDA]} progress messages. Default
\code{FALSE}.}
}
\value{
Object of class \code{c("sda_fit", "odacore_sda")}.
}
\description{
Executes staged attribute-set identification on binary class data.
Traverses the attribute space by class, selecting the best eligible
attribute at each step, removing correctly classified observations, and
repeating on the unresolved sample until a stopping condition is met.
The result identifies which attributes to pass to downstream CTA or MDSA.
}
