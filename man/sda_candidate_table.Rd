% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sda_interop.R
\name{sda_candidate_table}
\alias{sda_candidate_table}
\title{Return the candidate table from one or all SDA steps}
\usage{
sda_candidate_table(fit, step = NULL)
}
\arguments{
\item{fit}{An \code{sda_fit} object.}

\item{step}{Integer step index, or \code{NULL} (default) to return a list
of candidate tables for all steps.}
}
\value{
If \code{step} is an integer: the candidate table data frame for
  that step (with an added \code{step_id} column). If \code{step = NULL}:
  a named list of candidate table data frames, one per step.
}
\description{
The candidate table is the primary auditability record: one row per
candidate attribute evaluated at a step, showing ESS, p-value, eligibility,
and why a candidate was rejected or selected.
}
