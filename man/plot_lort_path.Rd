% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graphics_v3.R
\name{plot_lort_path}
\alias{plot_lort_path}
\title{Plot the full local CTA models along a LORT recursion path}
\usage{
plot_lort_path(
  x,
  index = 1L,
  layout = c("multipanel", "list"),
  ncol = 1L,
  target_class = 1L,
  color_by = c("none", "target_rate", "prediction"),
  label_detail = c("simple", "full"),
  show_node_ess = FALSE,
  show_p = TRUE,
  show_loo = TRUE,
  show_rule = TRUE,
  show_metrics = FALSE,
  short_edge_labels = TRUE,
  node_text_size = 3.5,
  edge_text_size = 3.2,
  palette = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{cta_ort} object from \code{\link{lort_fit}}.}

\item{index}{Integer; target LORT node index (end of path).}

\item{layout}{Character; \code{"multipanel"} (default) returns a single
arranged figure with one panel per path node -- requires the
\pkg{patchwork} package.  \code{"list"} returns the current named list of
ggplot objects.}

\item{ncol}{Integer; number of columns in the multipanel layout.  Default
\code{1L} (vertical stack).}

\item{target_class}{Integer; target class for node coloring.  Default
\code{1L}.}

\item{color_by}{Character; leaf fill mode.  Default \code{"none"}.}

\item{label_detail}{Character; \code{"simple"} (default) or \code{"full"}.}

\item{show_node_ess}{Logical.  Default \code{FALSE}.}

\item{show_p}{Logical; append \code{MC p = X.XXX} to split-node labels.
Default \code{TRUE}.}

\item{show_loo}{Logical; append LOO status/p to split-node labels.
Default \code{TRUE}.}

\item{show_rule}{Logical.  Default \code{TRUE}.}

\item{show_metrics}{Logical.  Default \code{FALSE}.}

\item{short_edge_labels}{Logical.  Default \code{TRUE}.}

\item{node_text_size}{Numeric.  Default \code{3.5}.}

\item{edge_text_size}{Numeric.  Default \code{3.2}.}

\item{palette}{Named list; color overrides.}

\item{...}{Ignored; reserved.}
}
\value{
With \code{layout = "multipanel"}: a single \code{patchwork}/ggplot
  object containing all path panels.  With \code{layout = "list"}: a named
  list of ggplot objects.
}
\description{
Returns a named list of ggplot objects, one per LORT node on the path from
the root to the requested \code{index}.  Each panel shows the \emph{full}
local CTA model embedded at that LORT node -- not a stump summary.
}
\details{
The list is named \code{index_1}, \code{index_2}, etc. (one name per LORT
node on the path).  Terminal nodes with no model get a message panel.
}
\seealso{
\code{\link{lort_index_path}}, \code{\link{lort_local_tree}},
  \code{\link{lort_path_table}}, \code{\link{plot_lort_tree}}
}
